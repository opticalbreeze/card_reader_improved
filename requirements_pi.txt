# Raspberry Pi版依存関係
# 仮想環境でインストールしてください: pip install -r requirements_pi.txt

# HTTP通信（サーバー送信用）
requests>=2.28.0

# NFCカード読み取り（nfcpy）
nfcpy>=1.0.4

# PC/SCカード読み取り
pyscard>=2.0.7

# GPIO制御（RGB LED、ブザー）
RPi.GPIO>=0.7.1

# I2C通信（LCD表示）
smbus2>=0.4.0

